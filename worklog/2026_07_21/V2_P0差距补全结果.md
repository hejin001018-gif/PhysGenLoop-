# V2 P0 差距补全实施结果

> 日期：2026-07-21
> 作者:lsh
> 基准：服务器 `/root/PhysGenLoop-`，main@13c6289
> 依据：`worklog/2026_07_21/PhysGenLoop_V2_具体实现差距审查.md`
> 原则：只新增/改参数默认值，不删旧代码；CPU 可测；GPU/ProPainter 验证待环境

---

## 一、本轮补齐的 P0 阻断项

| 编号 | 差距 | 修复 |
|---|---|---|
| P0-01 | Runner 因果链断裂：after candidate 未成为 current，before/after 未配对 | 重写 `runner.py` run() 循环：初始候选只生成一次；执行后 after candidate 成为下轮 current；RoundRecord 记 before/after candidate_id、physics、after_gate、execution_id、terminal_reason；after 立即 Re-Critic + Re-Gate |
| P0-02 | 非空 prompt 空 plan 被接受 | `runner._plan_ready()` + `require_plan` 配置：enforce 模式非空 prompt 空 plan 不接受 |
| P0-03 | Trial before/after 靠相邻 round 猜，概率伪造均匀 | 重写 `_assemble_trials` 为 execution-first：trial_id=execution_id；before/after 取 RoundRecord 自身配对字段；success 复合定义（execution succeeded 且 physics 提升）；概率按动作 one-hot 不伪造均匀；reject 无 after |
| P0-04 | force-action 未保证同源，可被首轮 accept 短路 | `RunnerConfig.force_action`：force 优先于 accept 短路；`build_v2_runner(force_action=)` 装配 forced decider 记 proposed vs forced；`run_actual_trials_v2` 四动作用同一 base_seed → 确定性同源 before candidate |
| P0-06 | Semantic scorer 不读视频，只发 prompt | 重写 `scorers_semantic.py`：cv2 抽关键帧 → base64 多模态发 Qwen3-VL；结构化输出（entity/event/temporal/new_object/reason/evidence/degraded）；不可用返回 available=False |
| P0-07 | vLLM 进程所有权不安全 | `build_backends` 启动 vLLM 后写 `vllm.owner.json`（pid/port/run_id）；`resource_coordinator.stop_owned_vllm` 精准停 |
| P0-08 | checkpoint hard gate 未执行 | 新增 `checkpoint_gate.py`：四层模式（disabled/proxy_research/actual_trial_research/deployment）；加载前硬判定，deployment 拒绝当前 proxy checkpoint；写 `checkpoint_gate.json` |
| P0-09 | SAM2 降级未贯穿 | `runner._is_degraded()` 检查 sam2_postprocess/fallback_used；critic_profiles 已写 degraded_reasons；enforce+fail_on_degraded 时纳入 gate |

## 二、新增/改动文件

| 文件 | 改动 |
|---|---|
| `generators/wanphysics/v2/runner.py` | 因果链重写；RoundRecord 扩展；plan gate；force_action |
| `generators/wanphysics/v2/checkpoint_gate.py` | 新增，分层 checkpoint 硬门禁 |
| `generators/wanphysics/v2/scorers_semantic.py` | 重写为真读视频关键帧 + 结构化输出 |
| `generators/wanphysics/v2/build_backends.py` | 接线 checkpoint gate / force_action / owner；ProPainter 路径改 models/ProPainter |
| `generators/wanphysics/v2/preflight.py` | ffmpeg 回退 imageio_ffmpeg；ProPainter 权重完整性检查（拒 .part / 空 .pth） |
| `agents/wanphysics/run_videophy2_loop_v2.py` | execution-first `_assemble_trials`；force_action 走装配 |
| `agents/wanphysics/run_actual_trials_v2.py` | 同源四动作 campaign |
| `generators/wanphysics/local_editor.py` | 默认路径 → models/ProPainter |
| `configs/loop_v2.yaml` | ProPainter 路径 + script/weights/python 字段；scorers 段 |
| `tests/wanphysics_v2/test_p0_gaps.py` | 新增，因果链/trial/checkpoint/semantic 测试 |

## 三、验证

```
466 passed（原 405 + V2 61），0 回退
四动作 dry-run 正常；checkpoint gate 正确拒绝 deployment、允许 proxy_research
preflight 正确识别 ProPainter 权重未下完（propainter_weights missing），local_editing 掩掉
```

## 四、ProPainter 状态与下一步

- ProPainter 已 clone 到 `models/ProPainter/`（README 合规位置，与 sam2-src 同层）；
- 权重**未下完整**：`weights/recurrent_flow_completion.pth.part` 是未完成临时文件，还缺主权重；
- 补下命令：
  ```bash
  cd /root/PhysGenLoop-/models/ProPainter
  /root/PhysGenLoop-/envs/main/bin/python scripts/download_models.py
  ls -la weights/   # 应有完整 ProPainter.pth / raft-things.pth / recurrent_flow_completion.pth
  ```
- 权重下完后 preflight 自动放行 local_editing → 可跑 force-local GPU smoke（P0-05 逐帧闭环验证，属 D 类 GPU 项）。

## 五、仍待 GPU/环境的项（本轮未做，纯代码已就绪）

- P0-05 Local Editing 逐帧闭环：需 ProPainter 权重 + GPU 真跑
- 四动作真实 GPU 各验证一次（force-action GPU smoke）
- P1 批（preflight 全资产 hash、config consumption test、RepairTrace 完整字段、schema 收紧等）：部分已随本轮完成，剩余待下一轮
- W6 smoke20 / W7 actual trials / W8 独立评估
