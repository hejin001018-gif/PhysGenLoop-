# 合并四人分支 + 目录整理 · 实施结果

> 日期：2026-07-18
> 作者：lsh
> 范围：把 hj / htz / sy 三个成员分支与 main 上的 worklog 合并到一起，并按 README 目录约定整理 `/root/PhysGenLoop-` 结构、把 `/root/benchmark` 资产物理搬入
> 执行方式：全程在 `integration` 分支操作，`main` 指针未动；未删任何文件；每步验证
> 结果：三分支全部合入，目录整理完成，全量测试 **405 passed / 0 failed**

---

## 一、起点状态

- 服务器 `main` = `f0ee212`（已先把 07_15 / 07_17 / 07_18 三份 worklog 提交进来）。
- 远端 GitHub `main`（`13573b9`）是一条被 `init` 压平、且 `.gitignore`/`pipeline.py` 质量更差的独立历史 → 以服务器 main 为准，远端已备份到 `/root/remote-main-backup`（bundle + 关键文件）。
- 三个成员分支（来自 `hejin001018-gif/PhysGenLoop-` fork）：

| 分支 | 基线 | 相对 main | 内容 | 冲突面 |
|---|---|---|---|---|
| **hj** | `e9b2eaf`（最新 main） | +8 / -0 | Learning Repair Agent 全套（`src/physgenloop/learning_repair/`、`configs/learning_repair/`、artifacts、Blender/SAM2 数据管道、docs/tests），53k+ 行纯新增 | 极小 |
| **htz** | 孤立历史（无共同祖先） | 单 commit | WanPhysics 视频生成器：15 prompts + 3 shell 脚本 | 无交集 |
| **sy** | `c87db4d`（仓库重组前） | +59 / -7 | `pavg_critic` 核心深改（15 文件）+ benchmark 工具 + critic trace + 全量 VideoPhy2 评测产物 | 中（旧基线 vs 重组） |

三分支先 fetch 到只读 `refs/audit/{hj,htz,sy}`，不碰 main / 工作树 / origin。

---

## 二、合并过程（integration 分支）

### 步骤 0：建 integration 分支
从 `main`(`f0ee212`) 建 `integration`，所有合并在其上做。

### 步骤 1：合并 hj — `9c48de0`
- hj 基于最新 main、几乎纯新增，直接 `merge --no-ff`。
- 唯一冲突：`learning_repair_agent_路线方案.md`（两份不同文档撞同一路径）→ 按决定**两份都留**：长版（456 行）保持原路径，hj 短版（70 行）另存为 `整合记录.md`。
- 108 文件、53198 行合入；artifacts 全保留（按决定）。
- `Blender_video/`、`SAM2_video/` 经查为脚本 + JSON/MD 报告，无真实视频，合理入库。

### 步骤 2：合并 htz — `b309a1d`
- 孤立历史，用 `merge --allow-unrelated-histories`。
- 按决定归档到 `generators/wanphysics/`（避免污染根目录），htz 的 `.gitignore` 独有规则（`videos/`、`*.mov`、`*.zip`）并入根 `.gitignore`。

### 步骤 3：合并 sy — `1461d0b`
- sy 从重组前旧基线分叉，但 Git rename 检测自动对齐了 docs 重组，**未产生预想的大量 rename 冲突**。
- 唯一冲突：`tests/test_schemas.py` — 取 sy 版（删掉引用已废弃 `generator_request.schema.json` 的 2 个测试，保留 sy 新增 critic_trace 测试）。**顺带修掉了 main 上长期存在的 2 个历史遗留失败。**
- outputs 全量评测产物按决定保留。

### 步骤 4：兼容性哈希重新冻结 — `1b81bcc`
- hj 的 learning_repair 兼容性门禁锚定旧哈希（hejin 训练时的 `default.yaml`/`critic_output.schema.json`），与团队 main 当前文件不符 → 1 个测试红。
- 确认该门禁完全隔离在 learning_repair 内部，不影响 main 任何逻辑。
- 三分支合并后 Critic 基线已定稿，按 hejin worklog 要求**重新冻结哈希**为当前真实值（`critic_config`→`f2b1f65…`、`critic_schema`→`534e608…`）。测试转全绿。

---

## 三、目录整理（B 方案：物理自包含）

按 README §2/§3/§7，把结构理清并把 `/root/benchmark` 资产物理搬入。

### 阶段 A：内部目录归位 — `edc1ebe` + 补丁 `4e3e3b1`
- `Blender_video/` → `data_pipeline/blender/`，`SAM2_video/` → `data_pipeline/sam2/`（git mv）。
- `artifacts/` → `outputs/repair_training/`（扁平化去掉双层嵌套；加 `.gitignore` 白名单例外，KB 级训练报告继续入库上 GitHub）。
- 修全部路径引用：4 shell 脚本、3 py 脚本、2 README、1 测试 import、1 proxy config；`.gitattributes`/`.gitignore` 同步。
- reports/ 里的历史归档记录（含旧 Windows 复现路径）**刻意不改**，保持可复现证据真实性。

### 阶段 B：大资产物理搬入（mv，不入库）
- `benchmark/models`（17G）→ `models/`
- `benchmark/data`（3.8G）→ `data/`
- `benchmark/runs`（291M）→ `outputs/benchmarks/runs/`
- 均被 `.gitignore` 挡住，git status 干净。

### 阶段 C：环境搬入 envs/（不入库）
- 因 uv 重装需重新下载 5-6G CUDA 依赖过慢，改用**物理搬迁 + 重定向**：
  - `benchmark/venv`（6.6G）→ `envs/main`，改 3 处指向：pavg_critic editable → `PhysGenLoop-/src`（integration 代码）、sam2 finder → 新 `models/sam2-src`、`bin/pavg-critic` shebang。
  - `vllm-venv`（9.1G）→ `envs/vllm`，`vllm-cu128-venv`（9.4G）→ `envs/vllm-cu128`（无指向依赖，直接搬）。
- `envs/main` 纯环境（无 PYTHONPATH）导入 pavg_critic / physgenloop / learning_repair / torch / cv2 / sam2 全部 OK。

### 阶段 D：docs 归档 + README 更新 — `d4940ac`
- 根 `docs/`（sy 带回的）按文件名日期分流：2 个 07-16 → `worklog/2026_07_16/docs/`（与已有合并）；13 个（8 个 07-17 + 2 个 learning-repair + 1 个 results）→ `worklog/2026_07_17/docs/`。
- README §3 目录树加 `envs/`，更新 data_pipeline/generators/outputs 注释，补边界说明，修复移动文档的死链。

---

## 四、最终结构（/root/PhysGenLoop-）

```
入库（上 GitHub）：
  src/  configs/  schemas/  benchmarks/  evaluation/  examples/  tests/
  data_pipeline/{blender, sam2}   generators/wanphysics/
  outputs/repair_training/（白名单）  worklog/  docs 已并入 worklog
  README.md  pyproject.toml  .gitignore  .gitattributes  .env.example

不入库（gitignore）：
  models/(17G)  data/(3.8G)  envs/{main,vllm,vllm-cu128}(25G)
  outputs/benchmarks/{runs, videophy2-full-qwen3vl8b}
```

## 五、integration 分支关键提交

```
d4940ac docs: archive root docs into worklog + update README
4e3e3b1 fix: update learning repair path references after directory move
edc1ebe refactor: reorganize directory structure per README layout
1b81bcc chore: refreeze learning repair compatibility hashes
1461d0b merge sy
b309a1d merge htz (-> generators/wanphysics/)
9c48de0 merge hj
f0ee212 docs: sync worklog（main 当前位置）
```

`main` 仍在 `f0ee212` 未动；integration 领先 71 个 commit。

## 六、验证

- 全量测试（纯 `envs/main`，无 PYTHONPATH）：**405 passed / 0 failed**。
- 合并前 main 的 2 个历史遗留失败（generator_request schema）已被 sy 版消除。
- hj 兼容性门禁已重新冻结转绿。
- 大资产全部被 gitignore，git 无污染。

## 七、尚未做（待决定）

1. **快进 main → integration**（`d4940ac`）+ **force-push 远端**（需 GitHub 凭证；远端 main 已备份）。
2. **`wan_generator.py`**（远端 main 里把 WanPhysics 通过 SSH 接进 physgenloop 闭环的桥）尚未接入，待定放置位置。
3. **清理清单**（仅列，未删）：`/root/benchmark` 剩余的过时 src 快照（`src` / `src-sy-c8d1810` / `report-src`，已确认无独有代码）、`logs`/`tmp`/`report-*` 临时物；`artifacts/`（4.6G，按决定留原地）；`refs/audit/*`；`/root/remote-main-backup`。

## 八、协作纪律遵守

- 全程在 integration 分支，main 未动，未 force-push。
- 未删任何文件（清理项只列清单）。
- 未改 Critic 判断语义；未把 proxy 结果包装为真实成功率。
- hejin 兼容性哈希按其 worklog 要求重新冻结，非擅自变更契约语义。
- 冲突文档两份都保留，不丢任何成员成果。
